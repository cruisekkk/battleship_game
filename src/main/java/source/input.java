package source;

import java.util.Scanner;

// the class is the unique variable throughout the execusion of the program
// final static can ensure the scanner just can be initialized once
class input
{
    final static Scanner reader = new Scanner(System.in);
}